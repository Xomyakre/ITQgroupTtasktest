version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: itq-postgres
    environment:
      POSTGRES_DB: ITQgroup
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - itq-postgres-data:/var/lib/postgresql/data

volumes:
  itq-postgres-data:

